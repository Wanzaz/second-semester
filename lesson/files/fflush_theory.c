#include <stdio.h>
#include <stdlib.h>

int main() {
	int num;
	scanf("%d", &num);
	/* fflush(stdin); */
	char character;
	scanf("%c", &character);


	/* stdin and stdout are streams of data */

	/* fprintf(stdout, "Hello"); = printf("Hello"); */

	return 0;
}


